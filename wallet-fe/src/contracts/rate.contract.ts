export type APIRate = {
  id: number;
  name: string;
  code: string;
  symbol: string;
  rate: number;
};

export type TRate = {
  id: number;
  name: string;
  code: string;
  symbol: string;
  rate: number;
};